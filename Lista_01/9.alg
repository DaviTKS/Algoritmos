algoritmo porcentagem

var 
    n1: inteiro
    desconto: real
    
inicio

    escreva("Digite um número: ")
    leia(n1)

    // Desconto de 10% sobre o valor final (ao invés do valor inicial)
    desconto <- n1 / 1.1

    escreva("O valor com desconto de 10% é: ", desconto)

fimalgoritmo